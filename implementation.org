#+title: Implementation

This file is retained as a pointer for older references.

The original scratch task list in this file described the pre-native
plan: create new AST/parser/evaluator modules, replace the old external
interpreter wrapper, and manually work through broad R5RS implementation
sections. That plan is obsolete.

The active implementation now lives on the stable datum parser, surface
classifier, native runtime, typed Rust standard library, REPL type query,
and occurrence-typing inference path.

Authoritative project direction and work tracking now live in:

- =README.org= for the concise project overview.
- =lavu-development.org= for architecture notes and the active work log.
- =AGENTS.md= for agent workflow and Rust style preferences.
